{
  "_comment": "API Gateway-style payload. Optional queryStringParameters.param. Lambda ENV follows stage (default prod). Examples: npx serverless invoke -f demo --path test.json  |  npm test",
  "queryStringParameters": {
    "param": "396"
  }
}
