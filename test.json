{
  "_comment": "API Gateway-style payload. Optional queryStringParameters.param. Lambda ENV follows stage (default prod). Examples: npm test  |  serverless invoke -f demo --path test.json --stage prod",
  "queryStringParameters": {
    "param": "396"
  }
}
